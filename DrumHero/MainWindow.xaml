<Window x:Class="DrumHero.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DrumHero.ViewModels"
        xmlns:views="clr-namespace:DrumHero.Views"
        d:DataContext="{d:DesignInstance vm:MainViewModel}"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding Title}"
        Height="768"
        Width="1200"
        MinHeight="600"
        MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Background="#1A1A2E"
        FontFamily="Segoe UI">

    <Window.Resources>
        <!-- DataTemplate mappings: ViewModel -> View -->
        <DataTemplate DataType="{x:Type vm:LibraryViewModel}">
            <views:LibraryView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PracticeViewModel}">
            <views:PracticeView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
            <views:SettingsView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:AnalysisProgressViewModel}">
            <views:AnalysisProgressView/>
        </DataTemplate>
    </Window.Resources>

    <!-- Content area: displays the current ViewModel's matched View -->
    <ContentControl Content="{Binding Navigation.CurrentViewModel}"/>
</Window>
