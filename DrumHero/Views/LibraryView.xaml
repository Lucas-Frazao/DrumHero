<UserControl x:Class="DrumHero.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:DrumHero.ViewModels"
             d:DataContext="{d:DesignInstance vm:LibraryViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700"
             d:DesignWidth="1000"
             Background="#1A1A2E">

    <UserControl.Resources>
        <Style x:Key="HeaderText"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="White"/>
            <Setter Property="FontSize"
                    Value="28"/>
            <Setter Property="FontWeight"
                    Value="Bold"/>
            <Setter Property="FontFamily"
                    Value="Segoe UI"/>
        </Style>
        <Style x:Key="SubText"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#B0B0B0"/>
            <Setter Property="FontSize"
                    Value="13"/>
            <Setter Property="FontFamily"
                    Value="Segoe UI"/>
        </Style>
        <Style x:Key="ActionButton"
               TargetType="Button">
            <Setter Property="Background"
                    Value="#0F3460"/>
            <Setter Property="Foreground"
                    Value="White"/>
            <Setter Property="FontSize"
                    Value="14"/>
            <Setter Property="FontFamily"
                    Value="Segoe UI"/>
            <Setter Property="Padding"
                    Value="20,10"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="Cursor"
                    Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="#1A4A8A"/>
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="#0A2A50"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DangerButton"
               TargetType="Button"
               BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background"
                    Value="#5C1A1A"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver"
                         Value="True">
                    <Setter Property="Background"
                            Value="#7A2A2A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0"
                   Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Right"
                        VerticalAlignment="Center">
                <Button Content="⚙ Settings"
                        Style="{StaticResource ActionButton}"
                        Command="{Binding OpenSettingsCommand}"
                        Margin="0,0,10,0"
                        Background="#2A2A4A"/>
                <Button Content="+ Add Song"
                        Style="{StaticResource ActionButton}"
                        Command="{Binding AddSongCommand}"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="🥁 Drum Hero"
                           Style="{StaticResource HeaderText}"/>
                <TextBlock Text="Select a song to start practicing"
                           Style="{StaticResource SubText}"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </DockPanel>

        <!-- Song count -->
        <TextBlock Grid.Row="1"
                   Style="{StaticResource SubText}"
                   Margin="0,0,0,10">
            <Run Text="{Binding Songs.Count, Mode=OneWay}"/>
            <Run Text=" songs in library"/>
        </TextBlock>

        <!-- Song List -->
        <Border Grid.Row="2"
                Background="#16213E"
                CornerRadius="8">
            <Grid>
                <!-- Empty state -->
                <StackPanel VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            Visibility="{Binding Songs.Count, Converter={StaticResource ZeroToVisibleConverter}, FallbackValue=Visible}">
                    <TextBlock Text="No songs yet"
                               Foreground="#606080"
                               FontSize="20"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,10"/>
                    <TextBlock Text="Click 'Add Song' to import a .flac file and drum MIDI from Songsterr"
                               Foreground="#505070"
                               FontSize="14"
                               HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Song list -->
                <ListView ItemsSource="{Binding Songs}"
                          SelectedItem="{Binding SelectedSong}"
                          Background="Transparent"
                          BorderThickness="0"
                          Margin="5"
                          ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Background"
                                    Value="Transparent"/>
                            <Setter Property="Foreground"
                                    Value="White"/>
                            <Setter Property="Padding"
                                    Value="15,12"/>
                            <Setter Property="Margin"
                                    Value="5,2"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                        <Border x:Name="Bd"
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="6"
                                                Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver"
                                                     Value="True">
                                                <Setter TargetName="Bd"
                                                        Property="Background"
                                                        Value="#1E2B4A"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected"
                                                     Value="True">
                                                <Setter TargetName="Bd"
                                                        Property="Background"
                                                        Value="#0F3460"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Title}"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="White"/>
                                    <TextBlock Text="{Binding Artist}"
                                               FontSize="12"
                                               Foreground="#808090"
                                               Margin="0,2,0,0"/>
                                </StackPanel>
                                <TextBlock Grid.Column="1"
                                           VerticalAlignment="Center"
                                           Margin="20,0"
                                           Foreground="#808090"
                                           FontSize="12">
                                    <Run Text="{Binding BPM, StringFormat='{}{0:F0} BPM'}"/>
                                </TextBlock>
                                <TextBlock Grid.Column="2"
                                           VerticalAlignment="Center"
                                           Margin="20,0"
                                           Foreground="#808090"
                                           FontSize="12"
                                           Text="{Binding Status}"/>
                                <StackPanel Grid.Column="3"
                                            Orientation="Horizontal"
                                            VerticalAlignment="Center">
                                    <Button Content="▶ Practice"
                                            Style="{StaticResource ActionButton}"
                                            Command="{Binding DataContext.OpenSongCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"
                                            Padding="12,6"
                                            FontSize="12"
                                            Margin="5,0"/>
                                    <Button Content="✕"
                                            Style="{StaticResource DangerButton}"
                                            Command="{Binding DataContext.RemoveSongCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                            CommandParameter="{Binding}"
                                            Padding="8,6"
                                            FontSize="12"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>
    </Grid>
</UserControl>
